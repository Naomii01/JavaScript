// Math round 
let num = 7.85;
let rounded = Math.round (num);
console.log (rounded);
// it rounds to the nearest integer depending where the decimal number is located 0-0.5 it will round down while from 0.5-0.9 it will round up 